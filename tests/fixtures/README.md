# Shared test fixtures

`upstream/` contains source and graph samples copied from the pinned Graphify
v0.9.32 test corpus. They are data consumed by Rust tests, not an embedded
Python test suite. Executable parity mappings and their provenance live under
`parity/`.
