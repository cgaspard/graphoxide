---
name: graphoxide
description: Query and maintain a deterministic code knowledge graph from Aider.
---

# Graphoxide

Use an existing `graphoxide-out/graph.json` before broad source searches. Run
`graphoxide query "<question>"`, `graphoxide explain <node>`, or `graphoxide path <a> <b>`.
Rebuild with `graphoxide update .` after structural changes.
