# Add and watch

Use incremental updates for a Graphoxide graph that already exists:

```bash
graphoxide watch .
graphoxide check-update .
graphoxide update .
```

`watch` keeps running and rebuilds after supported source files change. Use `update` for a single refresh.
