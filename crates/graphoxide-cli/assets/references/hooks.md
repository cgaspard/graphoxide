# Hooks

Graphoxide can maintain Git hooks and Claude Code project guidance:

```bash
graphoxide hook install .
graphoxide hook status .
graphoxide claude install .
```

Use the matching `uninstall` subcommand to remove only Graphoxide-owned integration blocks.
